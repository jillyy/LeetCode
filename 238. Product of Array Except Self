先把生成的array从左到右乘起来，再从右到左乘起来，把两样相乘
从左到右时arrya的第一个数仍然为0，需先设置成1

class Solution {
    public int[] productExceptSelf(int[] nums) {
        int leng = nums.length;
        int[] arr = new int[leng];
        arr[0] = 1;
        int left = 1;
        for (int i=1; i<leng; i++) {
            left = left * nums[i-1];
            arr[i] = left;
        }
        
        int right = 1;
        for (int i=leng-2; i>=0; i--){
            right = right * nums[i+1];
            arr[i] = right * arr[i];
        }
        return arr;
    }
}
